[project]
name = "project-name"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "bump-my-version==0.29.0",
    "ruff==0.9.3",
]

[tool.ruff]
target-version = "py313"
line-length = 120
